"use client";

import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    background: {
      default: "#09021C",
    },
  },
  typography: {
    fontFamily: "'Manrope', 'eUkraine', 'eUkraineHead'",
  },
});
